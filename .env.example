# AgentFund Protocol Environment Variables

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com
# For mainnet: https://api.mainnet-beta.solana.com

# Wallet (base64-encoded secret key)
# Generate with: solana-keygen new --no-bip39-passphrase --outfile wallet.json
# Then: cat wallet.json | base64
WALLET_SECRET_KEY=

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Jupiter API for token swaps
JUPITER_API_URL=https://quote-api.jup.ag/v6

# Optional: Webhook URL for payment notifications
WEBHOOK_URL=
WEBHOOK_SECRET=

# Optional: Database (for persistent storage)
# DATABASE_URL=postgresql://user:pass@localhost:5432/agentfund
